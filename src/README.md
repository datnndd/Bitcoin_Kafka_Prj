# Source Layout

- `src/producer/`: Binance WebSocket ingestion and Kafka producer.
- `src/common/`: Shared config and logging helpers for local producer code.

Databricks ETL code lives under `databricks/`, not `src/`.
