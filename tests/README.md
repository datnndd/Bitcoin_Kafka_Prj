# Test Layout

- `tests/fixtures/`: Sample Binance trade payloads.
- `tests/unit/`: Producer normalization and fixture replay tests.

Databricks pipeline files are syntax-checked with `py_compile`; runtime validation happens in Databricks.
